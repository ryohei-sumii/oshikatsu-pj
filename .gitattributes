# 改行コードをリポジトリ内で LF に統一し、チェックアウト時も LF のままにする
# （Windows でも LF にすることで "LF will be replaced by CRLF" の警告を防ぐ）
* text=auto eol=lf

# バイナリは変換しない
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
